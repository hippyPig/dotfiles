#!/bin/bash
# To execute some lines do:
#   eval "$(grep git stuffIAlwaysForget.sh)"

git config --global user.name "hippyPig"
git config --global user.email samcphall@gmail.com
git config --global core.editor vim
git config --global color.ui true
git config --global alias.co checkout
